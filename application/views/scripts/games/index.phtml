{
    "count" : "<?php echo $this->gameCount; ?>",
    "games" :
        [
            <?php
            $gamesJSONs = array();
            foreach ($this->games as $game) {
                array_push($gamesJSONs, $this->partial('game.phtml', array("game" => $game)));
            }
            echo join(",", $gamesJSONs);
            ?>
        ]
}
{
    "count" : "<?php echo $this->raceCount; ?>",
    "races" :
        [
            <?php
            $raceJSONs = array();
            foreach ($this->races as $race) {
                array_push($raceJSONs, $this->partial('race.phtml', array("race" => $race)));
            }
            echo join(",", $raceJSONs);
            ?>
        ]
}
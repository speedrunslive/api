{
    "channel" : <? echo json_encode($this->stream["channel"]); ?>,
    "api" : <? echo json_encode($this->stream["api"]); ?>,
    "whitelisted" : <? echo $this->stream["whitelisted"]; ?>,
    "met_race_req" : <? echo $this->stream["met_race_req"]; ?>
}

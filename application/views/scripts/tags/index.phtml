{
    "count" : "<?php echo count($this->tagGroups); ?>",
    "tag_groups" :
        [
            <?php
            $tagGroupsJSONs = array();
            foreach ($this->tagGroups as $tagGroup) {
                array_push($tagGroupsJSONs, $this->partial('taggroup.phtml', array("tagGroup" => $tagGroup)));
            }
            echo join(",", $tagGroupsJSONs);
            ?>
        ]
}